# keyross.yaml — the compiler configuration of this repository (it verifies itself)
gauges: [core, einvoice]
adapters: []
oracles_dir: oracles
badset_dir: badset
context:
  units: [u, m, m2, m3, ml, kg, t, ens, ff, h, j, l]
report_dir: .keyross/reports
