# Keyross-oss/conformance — CI action (0.4)

`uses: Keyross-oss/conformance@v1`: creates an ephemeral kind cluster, deploys the agent image with the chart, runs the conformance Job, returns the report and the exit code.
